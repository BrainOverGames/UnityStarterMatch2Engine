namespace BOG
{
    /// <summary>
    /// Responsible to handle popup functionality when game has ended(win)
    /// </summary>
    public class WinPopup : EndGamePopup
    {
        protected override void Awake()
        {
            base.Awake();
        }
    }
}
